손실 함수에 대한 파악

26년 1학기때 배운 신경망 다층 퍼셉트론은 정답이 있고 그것에 따라 훈련을 시키는 간단한 MSE(평균제곱오차)를 활용한 신경망을 만들었다
여기서 정답이 없는 문제들에 대해서는 어떠한 훈련과 학습을 할까?

-PINNS의 손실함수 구성
예를 들어 2차원 파동방정식을 생각해보자.


$$u_{tt}=c^2(u_{xx}+u_{yy})$$


이므로 식을 넘기면 $u_{tt}-c^2(u_{xx}+u_{yy})=0$ 이므로

식 $u(x,y,t)=u_{tt}-c^2(u_{xx}+u_{yy})$ 로 두고 $u(x,y,t)$가 0이 될 상황을 목표로 하여 MSE를 활용한 손실함수를 구성해보자.

$$L_{PDE} = \frac{1}{N}\sum_i r_\theta(x_i,y_i,t_i)^2$$


로 구성된다.

하지만 오차는 $L_{PDE}$로만 끝나지 않는다. PDE 특성상 초기조건과 경계조건을 같이주어야한다.
PDE 딱 하나만 던져주게 되면 해는 하나가 아니라 여러 개일 수 있기때문이다.

이번에는 예를 들어 $0 \le x \le 1, 0 \le y \le 1$에서 초기 변위


$$u(x,y,0) = \sin(\pi x)\sin(\pi y)$$


초기 속도


$$u_t(x,y,0)=0$$


경계


$$u(0,y,t)=u(1,y,t)=0$$

$$u(x,0,t)=u(x,1,t)=0$$


를 가진다고 하자.

이를 통해 우리가 찾고있는 특정한 해를 찾을 수 있다.
참고로 정확한 해를 알게된다면
$u(x,y,t)=\sin(\pi x)\sin(\pi y)\cos(\sqrt{2}\pi ct)$ 로 구할수 있다. 즉, PINN이 근사하려는 함수가 바로 이 함수다.
PINN이 근사하려는 함수는 $\hat{u}_\theta$ 으로 정의하겠다.

일반적인 신경망은 이렇게 손실함수를 구성한다. $(\text{PINN이 근사한 함수} - \text{실제 함수})^2$
하지만 PINN은 정답 함수 없이 $u_{tt}-c^2(u_{xx}+u_{yy})$가 0에 근접하는지 확인하여 손실함수를 구성한다.
결국은 MSE기반의 손실함수임을 알 수 있다.

그리고 초기조건, 경계조건의 변화도 확인해야한다.
처음에 주어진 초기조건과 경계조건을 얼마나 만족 하는지 확인해야한다
먼저 초기조건 $L_{IC}$이다. 역시나 여기서도 MSE 방법을 이용한다. 초기 변위는


$$u(x,y,0) = \sin(\pi x)\sin(\pi y)$$


이므로 $\hat{u}_\theta(0)^2$ ($u_0$는 초기값)으로 잡는다.

초기속도는 신경망의 시간 미분
$\frac{\partial \hat{u}_\theta}{\partial t}$
의 초기속도 $\nabla v_0(x,y)$와 같아야 한다.

$$
\left(
\frac{\partial \hat{u}_\theta}{\partial t}
- v_0(x,y)
\right)^2
$$

즉 전체 $L_{IC}$는

$$
L_{IC} = L_{\text{초기변위}} + L_{\text{초기속도}}
$$

다음은 경계조건 $L_{BC}$이다. 경계조건을

$$
u(0,y,t) = u(1,y,t) = 0
$$

$$
u(x,0,t) = u(x,1,t) = 0
$$

으로 설정했으므로 각 경계에서 예측값이 0이 되도록 MSE를 계산하면 된다.

예를 들어 $x=0$ 경계에서는

$$\boxed{ L_{BC,x=0} = \frac{1}{N_{BC}} \sum_i \left[ \hat{u}_\theta(0,y_i,t_i)-0 \right]^2 }$$

$x=1$에서는

$$\boxed{ L_{BC,x=1} = \frac{1}{N_{BC}} \sum_i \left[ \hat{u}_\theta(1,y_i,t_i)-0 \right]^2 }$$

$y=0$에서는

$$\boxed{ L_{BC,y=0} = \frac{1}{N_{BC}} \sum_i \left[ \hat{u}_\theta(x_i,0,t_i)-0 \right]^2 }$$

$y=1$에서는

$$\boxed{ L_{BC,y=1} = \frac{1}{N_{BC}} \sum_i \left[ \hat{u}_\theta(x_i,1,t_i)-0 \right]^2 }$$

따라서 전체 경계조건 Loss는

$$\boxed{ L_{BC} = L_{BC,x=0} + L_{BC,x=1} + L_{BC,y=0} + L_{BC,y=1} }$$

이다.

결국 셋의 구조를 다 이해한 후 최종적인 손실함수를 구할 수 있다.

$$L_{total} = w_{PDE} L_{PDE} + w_{BC} L_{BC} + w_{IC} L_{IC}$$

1. PDE LOSS : 이 함수가 파동방정식을 만족하는가?
2. $L_{IC}$ : 처음 t=0에서 주어진 상태와 같은가?
3. $L_{BC}$ : 경계에서 주어진 조건을 만족하는가?
